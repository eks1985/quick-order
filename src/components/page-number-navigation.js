import React from 'react';
import { connect } from 'react-redux';

const PageNumberNavigation = props => {
  return (
    <div>
      PageNumberNavigation
    </div>
  );
}

export default connect()(PageNumberNavigation);
